console.log('hello, world');

var h1Selected = document.querySelector('h1');
console.log(h1Selected);
console.dir(h1Selected);

var expSelected = document.querySelector('#explanation');
console.log(expSelected);
console.dir(expSelected);

var hintSelected = document.querySelector('.hint');
console.log(hintSelected);
console.dir(hintSelected);

var pListSelected = document.querySelectorAll('p');
console.log(pListSelected);

var exListSelected = document.querySelectorAll('.example-link');
console.log(exListSelected);
