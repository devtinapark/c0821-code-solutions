/* global LinkedList */ // eslint-disable-line no-unused-vars
/* exported insertNext */

function insertNext(list, value) { /*
  const newNode = list;
  if (list.next === null) return;
  list.next.data = value;
  if (list.next.next === null) return;
  list.next.next = newNode.next;
  */
}
